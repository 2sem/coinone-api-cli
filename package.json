{
  "name": "coinone-api-cli",
  "version": "1.0.5",
  "description": "Developer-friendly CLI for Coinone public APIs.",
  "homepage": "https://github.com/2sem/coinone-api-cli",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/2sem/coinone-api-cli.git"
  },
  "bugs": {
    "url": "https://github.com/2sem/coinone-api-cli/issues"
  },
  "type": "module",
  "bin": {
    "coinone": "./dist/bin/coinone.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "cli": "tsx src/bin/coinone.ts",
    "docs:generate-reference": "node --import tsx ./scripts/generate-command-reference.mts",
    "docs:dev": "npm run docs:generate-reference && vitepress dev docs",
    "docs:build": "npm run docs:generate-reference && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "coinone",
    "cli",
    "automation",
    "trading",
    "coinone-api",
    "cryptocurrency",
    "api"
  ],
  "license": "MIT",
  "dependencies": {
    "commander": "^14.0.1"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitepress": "^1.6.4",
    "vitest": "^3.2.4"
  }
}
